# Unia 房管系统 — 前端

基于 React + Vite 构建。生产模式下由后端统一托管，无需单独部署。

## 开发

```bash
npm install
npm run dev      # 开发服务器，默认 http://localhost:3000
npm run build    # 构建到 dist/，供后端静态托管
```

详细部署说明见项目根目录 [README.md](../README.md)。
